# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vercel
.vercel/
.vercel

# Backups (NUNCA commitear dumps de DB — pueden tener datos sensibles)
*.dump
*.sql.gz
backups/
jarvex-backups/

# Claude Code local config (per-developer, no project-level)
# Las skills "globales" (.agents/skills) sí se commitean para todo el equipo;
# los symlinks de .claude/skills cada dev los regenera con `npx skills add`.
.claude/

# Google Drive (secretos — NUNCA commitear)
credenciales*.json
**/credenciales*.json
.drive-config.json
scripts/google-drive/.drive-config.json
oauth-client*.json
scripts/google-drive/oauth-client.json

# Modelos de referencia que deja Gabriel (Excel/fotos de órdenes, activos…).
# NO se commitean: el repo es PÚBLICO y esos archivos traen N° de cuenta, CCI,
# DNI del representante legal y RUCs del grupo. Para pasarlos a la otra PC:
# Drive/WhatsApp, no git. (Tanda 5, 4-sep-2026.)
Modelos/
